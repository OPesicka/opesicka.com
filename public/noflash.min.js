/* eslint-disable no-sequences */
/* eslint-disable no-unused-expressions */
!(function () {
  let b
  let a
  const c = 'dark-mode'
  const d = 'light-mode'
  const e = '(prefers-color-scheme: dark)'
  const f = window.matchMedia(e)
  const h = f.media === e
  try {
    const g = localStorage.getItem('scheme-mode')
    g && (a = JSON.parse(g))
  } catch {
    /* empty */
  }
  ;(b =
    h && a === 'system'
      ? f.matches
      : (a === 'light' || a === 'dark') && a === 'dark'),
    document.querySelector('html').classList.add(b ? c : d),
    document.querySelector('html').classList.remove(b ? d : c)
})()
